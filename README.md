# The Insitorian

Elections coverage for Prosperous Universe.

## Commands

```bash
npm install          # install dependencies
npx eleventy --serve # dev server with live reload
npx eleventy         # production build (output: _site/)
```

## Updating voting data

Edit `src/_data/voting.csv` — the build parses it automatically.
